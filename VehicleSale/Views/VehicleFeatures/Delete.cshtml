@model ClassLibrary1.VehicleFeature

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    var ID = ViewBag.ID;
}

<section class="panel panel-info">
    <header class="panel-heading">
        <h3 class="panel-title">Vehicle Features</h3>
    </header>
    <div class="panel-body">

        <h3>Are you sure you want to delete this?</h3>
        <div>

            <dl class="">


                <dt>
                    @Html.DisplayNameFor(model => model.FeaturesMaster.Title)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.FeaturesMaster.Title)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Vehicle.Title)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Vehicle.Title)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-primary" /> |
                    @Html.ActionLink("Back to List", "Index", "VehicleFeatures", new { id = ID }, new { @class = "btn btn-primary" })
                </div>
            }
        </div>
    </div>
</section>